services:
  api:
    build: .
    ports:
      - '3000:3001'
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on:
      - redis
    volumes:
      - ./db:/usr/src/app/db

volumes:
  redis_data:
